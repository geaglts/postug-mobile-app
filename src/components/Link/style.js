import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        margin: 8,
    },
    link: {
        color: "#6c63ff",
        fontSize: 14,
        fontWeight: "bold",
        letterSpacing: 1,
        textTransform: "uppercase",
    },
});
