import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    button: {
        color: "white",
        backgroundColor: "#6c63ff",
        fontSize: 22,
        margin: 10,
        padding: 10,
        borderRadius: 10,
        textAlign: "center",
    },
});
